export class NavOption {
  label: string;
  url: string;
  topBar: string;
   constructor(label: string, url: string, topBar: string) {
     this.label = label;
     this.url = url;
     this.topBar = topBar;
   }
}
